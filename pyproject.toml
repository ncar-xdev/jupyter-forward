[tool.black]
line-length = 100
target-version = ['py38']
skip-string-normalization = true

[tool.check-manifest]
ignore = ["docs/*", "tests/*", "ci/*"]

[tool.nbqa.mutate]
isort = 1
pyupgrade = 1

[build-system]
requires = ["setuptools>=45", "wheel", "setuptools_scm>=6.2"]
